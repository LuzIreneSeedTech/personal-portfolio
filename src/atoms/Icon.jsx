import React from 'react'

const Icon = ({props}) => {
  // const { iconImage } = props;

  return (
    <div className='icon-container'>
      {/* {iconImage} */}
    </div>
  )
}

export default Icon
